#include <iostream>
#include <vector>

using namespace std;

#define MOD 998244353

int solution(int n, int m, int idx) {
    int ans=0;

    if(idx == n) {

    }



    return ans;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, m;
    cin>>n>>m;

    int ans = solution(n, m, 0);
    cout<<ans<<"\n";

    return 0;
}
