#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

char* readline() {
    
}

char** split_string(char* str, char* delimiters) {

}

int* solution(int** arr, size_t arr_rows, size_t arr_cols) {
    int* ans;

    return ans;
}

int main() {
    char delimiters[] = {"[],
    return 0;
}

