#include <bits/stdc++.h>
using namespace std;

int solve(vector<int> arr) {
    int ans=1;
    int s=arr.size();

    for(int i=1; i<s; ++i) {
        

    return ans;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin>>n;

    vector<int> a(n);
    for(int i=0; i<n; ++i)
        cin>>a[i];

    int ans=solve(a);
    cout<<ans<<"\n";

    return 0;
}
